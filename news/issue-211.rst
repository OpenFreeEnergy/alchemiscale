**Added:**

* Durable execution provenance for ``Task``\s via per-attempt ``TaskProvenance`` records, surfaced through ``AlchemiscaleClient.get_task_history`` and ``AlchemiscaleClient.get_tasks_details``.
